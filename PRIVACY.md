# 隐私说明

家教群消息可能包含未成年人信息、家庭住址、手机号、学校、报价、微信标识和聊天原文。这些内容具有高度敏感性。

## 本项目的默认边界

- 只调用本机 loopback 地址上的 WeFlow HTTP API。
- 默认关闭地图通勤请求；不开启时，订单地址和出发地坐标不会发送给地图服务。
- 默认桌面通知只显示单号、年级和科目，不显示订单地址。
- 报告、状态、地图缓存和聊天原文只保存在本机，不会由本项目整体上传。
- 不自动发送微信消息，不自动申请订单。
- `config.local.json`、`state/`、`reports/`、`logs/` 和导入表格均被 Git 忽略。
- WeFlow token 从其本地配置或环境变量读取，不打印到检查结果。
- 地图 AK 只从环境变量读取。

## 地图通勤是可选的第三方传输

实时监控只有同时满足以下条件时，程序才会请求百度地图：

1. 本地配置中的 `map_routing_enabled` 被明确设为 `true`；
2. 环境变量 `BAIDU_MAP_AK` 已配置；
3. 本轮仍有新的线下地址需要计算。

手动 Markdown 命令的 `--route-limit` 默认是 `0`，不会请求地图。只有你显式把它设为大于 `0`，同时提供 GCJ-02 出发地坐标并配置 `BAIDU_MAP_AK` 时，手动模式才会发送地址。

开启后，程序会通过 HTTPS 把从群消息中抽取的上课地址发送给百度地图地理编码服务，并把你的出发地坐标和目标坐标发送给路线服务。地址可能对应未成年人的家庭、学校或常去地点；百度地图会按其自身服务条款和隐私政策处理这些请求。请先确认你有权这样处理信息，优先使用地铁站、商圈等模糊地点，不要提交门牌号、楼栋或房间号。

地图请求关闭或失败时，订单仍会被解析和排序，但新的线下地址不会得到实时距离、通勤时间和净时薪。地图缓存保存在本机 `state/` 中，其中可能包含地址和坐标，应按敏感数据保护。

## 桌面通知

`notification_include_address` 默认为 `false`。若改为 `true`，订单地址会出现在 Windows 通知正文中，并可能在锁屏、共享屏幕或录屏中被他人看到。除非设备和使用环境完全私密，否则不要开启。

## 使用者责任

1. 只处理你本人有权访问和处理的群消息。
2. 不要把真实报告、状态目录、截图或本地配置提交到 GitHub。
3. 分享结果前删除住址、联系方式、群 ID、发送者 ID 和未成年人信息。
4. 关闭 WeFlow 对非本机地址的监听；本项目会拒绝连接非 loopback API。
5. 开启地图前确认地址处理权限，并尽量把地址模糊到地铁站或商圈级别。
6. 发现中介费、押金、证件或其他异常要求时先人工核验，谨防诈骗。

如果隐私文件曾经进入 Git 历史，仅删除当前文件并不够。请立即将仓库设为私有、轮换相关密钥，并使用 Git 历史清理工具彻底移除。
